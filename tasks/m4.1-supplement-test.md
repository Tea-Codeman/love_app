# M4.1 埋点补测清单（闭合 13 事件）

M4.1 真机验收（2026-08-30 凌晨，查库 26 条）已 PASS，但 `chat_unlocked` / `profile_completed` / `recommend_view` 三个事件计数为 0——经代码核查均为「测试数据/路径未覆盖」而非实现错误。本清单用于一轮跑完补测、闭合 13 事件。

> 跑完告诉我，Agent 直接查云端 `events` 集合按 `eventName` 分组核对，断言三件事：① 三事件是否出现；② PII 零泄漏；③ 白名单无越界。

---

## 轮次 1 · 验证 `chat_unlocked`（需全新未聊过的 pair）

**为什么之前没触发**：`chat_unlocked` 语义是「本 pair 在 `messages` 的**历史首条**消息才报」（`chat/index.js:148` `isFirstMessage=!m`）。`6LrPFY↔sJ8Fv8` 在 M3 已互聊过，`MESSAGES_COL` 早有记录，故本次不算首条。

**步骤**：
1. 用**两个此前从未在本小程序互聊过**的微信号 C / D 登录（或一个全新号 + 一个旧号但二者间无消息记录）。
2. C 给 D 发**第一条**消息（过审后入库）。
3. 预期：`events` 新增 1 条 `chat_unlocked`（带 `growthValue`）。

**注意**：不要复用 `6LrPFY↔sJ8Fv8` 这对，它会永远不算首条。

---

## 轮次 2 · 验证 `profile_completed`（需填齐四项资料）

**为什么之前没触发**：触发条件 `isProfileComplete` = 昵称 + 头像 + 性别 + **年龄(`Number(age)>0`)** 齐全（`auth/index.js:106`）。两个测试号资料都不完整（sJ8Fv8 只存了 mbti）。

**步骤**：
1. 任一账号进入「我的资料」编辑页。
2. **同时填齐**：昵称、头像（上传一张）、性别（选男/女）、年龄（填一个 >0 的数字，如 25）。
3. 点击保存（触发 `updateProfile`，且 patch 含 `mbti` 则同时报 `mbti_completed`）。
4. 预期：`events` 新增 1 条 `profile_completed`。

**踩坑提示**：只填昵称/头像/性别不算完成——**年龄必须填且 >0**，否则 `isProfileComplete` 返回 false。

---

## 轮次 3 · 验证 `recommend_view`（需进入推荐/匹配页）

**为什么之前没触发**：`recommend_view` 上报仅在进入 `match.vue` 推荐流、调用 `loadCandidates` 时触发；本轮你走的是「接受推荐」流程，没进推荐页浏览。`app_open` 能报证明前端 `track.js` 已重建运行，只是没走到该埋点。

**步骤**：
1. 进入小程序「匹配 / 推荐」页（浏览候选列表，触发 `loadCandidates`）。
2. 预期：`events` 新增 1 条 `recommend_view`。

**踩坑提示**：只接受推送、不主动进推荐页浏览是不会报的；必须真的进页拉取候选。

---

## 可选 · 顺带验证 `report_created`（举报链路，仅 `targetType` 不报 `reason`）

1. 在任意会话/用户页点「举报」，选择举报类型，填理由文本（自由文本）。
2. 预期：`events` 新增 1 条 `report_created`，其 `props` **只含 `targetType`**，**绝不含 `reason`/微信号/昵称**（PII 过滤核验）。

---

## 验收通过标准（全部满足即 M4.1 全闭）

- [ ] `chat_unlocked` ≥ 1（全新 pair 首条消息）
- [ ] `profile_completed` ≥ 1（四项齐全保存）
- [ ] `recommend_view` ≥ 1（进推荐页）
- [ ] （可选）`report_created` ≥ 1 且 `props` 无 `reason`
- [ ] 全量 26+ 条中 `props` 无 PII（reason/微信号/昵称/消息内容）
- [ ] 无白名单外 `eventName`

---

## 🟡 已知非阻塞瑕疵（补测后一并评估是否修）

`contact_unlocked` 每次 `chat.contact` 成功都报，**未幂等**（真机报了 3 次），会放大 SC3 分母。建议加 pair 维度首次标记（如 `contactUnlocked:true`），可顺手在补测轮次中修，或留 M4.x。
